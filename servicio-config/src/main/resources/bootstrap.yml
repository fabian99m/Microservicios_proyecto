server:
  port: 8090

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fabian99m/Microservicios_proyecto.git
          searchPatchs: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: adm1n